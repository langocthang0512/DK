# Local Development

Start the development server:

```bash
npm run dev
```

Vite provides hot reload, source maps, and the browser error overlay during development.

Environment variables exposed to client code must be prefixed with `VITE_`. Add new public variables to `.env.example` whenever they are introduced.

Debug features are controlled by `VITE_DEBUG`.
